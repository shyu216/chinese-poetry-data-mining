/**
 * @overview
 * file: web/src/generated/word-similarity/word-similarity-file.ts
 * category: generated
 * tech: TypeScript + FlatBuffers
 * solved: 提供自动生成的数据结构解析与访问接口
 * data_source: FlatBuffers 二进制结构
 * data_flow: 二进制缓冲区 -> schema 访问器 -> 结构化字段读取
 * complexity: 字段读取常见 O(1)，向量遍历 O(n)
 * unique: 核心导出: WordSimilarityFile
 */
// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { WordEntry } from '../word-similarity/word-entry.js';

export class WordSimilarityFile {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WordSimilarityFile {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWordSimilarityFile(bb:flatbuffers.ByteBuffer, obj?:WordSimilarityFile):WordSimilarityFile {
  return (obj || new WordSimilarityFile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWordSimilarityFile(bb:flatbuffers.ByteBuffer, obj?:WordSimilarityFile):WordSimilarityFile {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WordSimilarityFile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('WRSV');
}

words(index: number, obj?:WordEntry):WordEntry|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new WordEntry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

wordsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

vocab(index: number):string|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  if (!offset) return null;
  const result = this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4);
  return typeof result === 'string' ? result : null;
}

vocabLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startWordSimilarityFile(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addWords(builder:flatbuffers.Builder, wordsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, wordsOffset, 0);
}

static createWordsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startWordsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addVocab(builder:flatbuffers.Builder, vocabOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, vocabOffset, 0);
}

static createVocabVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startVocabVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endWordSimilarityFile(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishWordSimilarityFileBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'WRSV');
}

static finishSizePrefixedWordSimilarityFileBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'WRSV', true);
}

static createWordSimilarityFile(builder:flatbuffers.Builder, wordsOffset:flatbuffers.Offset, vocabOffset:flatbuffers.Offset):flatbuffers.Offset {
  WordSimilarityFile.startWordSimilarityFile(builder);
  WordSimilarityFile.addWords(builder, wordsOffset);
  WordSimilarityFile.addVocab(builder, vocabOffset);
  return WordSimilarityFile.endWordSimilarityFile(builder);
}
}
