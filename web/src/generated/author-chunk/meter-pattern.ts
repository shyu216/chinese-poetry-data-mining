/**
 * @overview
 * file: web/src/generated/author-chunk/meter-pattern.ts
 * category: generated
 * tech: TypeScript + FlatBuffers
 * summary: FlatBuffers 生成的访问器，用于读取作者相关的格律/句式模式（MeterPattern）。
 *
 * Data pipeline (binary access):
 *  - 输入: FlatBuffers ByteBuffer
 *  - 处理: 使用自动生成的 getter 获取字符串与计数（pattern, count）
 *  - 输出: 在不解码为完整对象的前提下提供按需访问
 *
 * Complexity & notes:
 *  - 单字段访问为 O(1)，向量访问为 O(n)。批量解码会分配 JS 对象，可能带来内存与 GC 成本
 *  - 此文件自动生成，请通过 FlatBuffers schema 重新生成以修改结构
 */
// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class MeterPattern {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MeterPattern {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMeterPattern(bb:flatbuffers.ByteBuffer, obj?:MeterPattern):MeterPattern {
  return (obj || new MeterPattern()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMeterPattern(bb:flatbuffers.ByteBuffer, obj?:MeterPattern):MeterPattern {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MeterPattern()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

pattern():string|null
pattern(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
pattern(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

count():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startMeterPattern(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPattern(builder:flatbuffers.Builder, patternOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, patternOffset, 0);
}

static addCount(builder:flatbuffers.Builder, count:number) {
  builder.addFieldInt32(1, count, 0);
}

static endMeterPattern(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMeterPattern(builder:flatbuffers.Builder, patternOffset:flatbuffers.Offset, count:number):flatbuffers.Offset {
  MeterPattern.startMeterPattern(builder);
  MeterPattern.addPattern(builder, patternOffset);
  MeterPattern.addCount(builder, count);
  return MeterPattern.endMeterPattern(builder);
}
}
