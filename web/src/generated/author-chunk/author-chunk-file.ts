/**
 * @overview
 * file: web/src/generated/author-chunk/author-chunk-file.ts
 * category: generated
 * tech: TypeScript + FlatBuffers
 * summary: 自动生成的 AuthorChunkFile accessor，表示作者分片文件的顶级结构（包含 authors 数组、chunkIndex、totalAuthors）。
 *
 * Data pipeline (binary access):
 *  - 输入: 已加载的 FlatBuffers 二进制缓冲区
 *  - 处理: 使用自动生成的 accessor 逐项访问 `authors` 向量中的 `Author` 对象
 *  - 输出: 通过 `authors(index)` 等方法按需读取嵌套记录，避免一次性解析所有对象
 *
 * Complexity & notes:
 *  - 访问单个作者 O(1)，遍历所有作者 O(n)。若需批量读取大量作者，请在 Worker 中进行并注意内存分配。
 *  - 文件为自动生成代码，请通过修改 FlatBuffers schema 并重新生成来变更结构
 */
// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Author } from '../author-chunk/author.js';


export class AuthorChunkFile {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AuthorChunkFile {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAuthorChunkFile(bb:flatbuffers.ByteBuffer, obj?:AuthorChunkFile):AuthorChunkFile {
  return (obj || new AuthorChunkFile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAuthorChunkFile(bb:flatbuffers.ByteBuffer, obj?:AuthorChunkFile):AuthorChunkFile {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AuthorChunkFile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('ATCH');
}

authors(index: number, obj?:Author):Author|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Author()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

authorsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

chunkIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

totalAuthors():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startAuthorChunkFile(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addAuthors(builder:flatbuffers.Builder, authorsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, authorsOffset, 0);
}

static createAuthorsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAuthorsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addChunkIndex(builder:flatbuffers.Builder, chunkIndex:number) {
  builder.addFieldInt32(1, chunkIndex, 0);
}

static addTotalAuthors(builder:flatbuffers.Builder, totalAuthors:number) {
  builder.addFieldInt32(2, totalAuthors, 0);
}

static endAuthorChunkFile(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishAuthorChunkFileBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'ATCH');
}

static finishSizePrefixedAuthorChunkFileBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'ATCH', true);
}

static createAuthorChunkFile(builder:flatbuffers.Builder, authorsOffset:flatbuffers.Offset, chunkIndex:number, totalAuthors:number):flatbuffers.Offset {
  AuthorChunkFile.startAuthorChunkFile(builder);
  AuthorChunkFile.addAuthors(builder, authorsOffset);
  AuthorChunkFile.addChunkIndex(builder, chunkIndex);
  AuthorChunkFile.addTotalAuthors(builder, totalAuthors);
  return AuthorChunkFile.endAuthorChunkFile(builder);
}
}
