# 2026-03-12 核心库验证与确定性优化

## 当时的尝试

验证项目中所有核心库的经典用法是否正常工作，确保环境稳定。

## 关键验证结果

| 库 | 状态 | 用途 |
|---|---|---|
| Pandas/NumPy | ✅ | 数据处理 |
| OpenCC/pypinyin | ✅ | 繁简转换、拼音 |
| scikit-learn | ✅ | TF-IDF、聚类、PCA |
| Plotly | ✅ | 可视化 |
| NetworkX | ✅ | 社交网络分析 |
| gensim | ✅ | Word2Vec词向量 |
| HanLP/JioNLP | ❌ | 计划未来添加 |

## 关键创新：确定性优化

**目标**: 彻底移除容错机制，确保数据处理确定性

**原因**: 避免不同人复现出不同结果

**做法**:
- 所有import语句移到文件开头
- 删除所有try-except结构
- 所有依赖包必须安装，否则直接报错
- 不再有"跳过"或"降级"逻辑

## 环境配置确立

创建 `environment.yml`，锁定关键版本:
- `numpy=1.24` - 兼容性
- `setuptools=69.5.1` - node2vec需要
- `pandas=2.0` - 稳定版本

## Bug修复记录

修复 `poetry_knowledge_base.html`:
- 模板字符串引号嵌套错误
- 添加错误处理和resp.ok检查
